version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  build:
    commands:
      - java -version
      - ls -a
      - mvn package
artifacts:
  files:
    - '**/*'
  base-directory: 'webapp/target/webapp'